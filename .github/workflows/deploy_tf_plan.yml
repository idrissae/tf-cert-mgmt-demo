name: deploy tf plan contains encrypted file

on: [push]

jobs:
    decrypt-certif-file:
      runs-on: ubuntu-18.04
      steps:
        - uses: actions/checkout@v3
        - name: ls
          run: ls
          


    